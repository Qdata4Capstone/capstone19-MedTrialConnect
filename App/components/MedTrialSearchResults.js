import React, { Component } from "react";
import { Container, Header, Left, Icon, Right, Button, Body, Content, Text, Item, Label, Input } from "native-base";

export default class TrialSearchResults extends Component {
  render() {
    return (
      <Container>
        <Content padder>
        </Content>
      </Container>
    );
  }
}